name: Deploy Stable
on:
  release:
    types: [released]
jobs:
  build-and-deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    secrets: inherit
    with:
      artifact-retention-days: 7
      channelId: live