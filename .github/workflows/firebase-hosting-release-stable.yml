name: Deploy Stable
on:
  release:
    types: [released]
jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      artifact-retention-days: 7
  deploy:
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      channelId: live